		<view class="houseType">
			<view class="houseType-item" wx:for="{{houseItem}}" data-item="{{item}}" data-index="{{index}}" wx:key="index" bindtap="tabItem">
				<van-icon size="80rpx" name="{{item.icon}}" />
				<text class="houseType-item-name">{{item.name}}</text>
			</view>
		</view>