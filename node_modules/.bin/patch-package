#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_patch-package@6.2.2@patch-package/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../_patch-package@6.2.2@patch-package/index.js" "$@"
  ret=$?
fi
exit $ret
